interface:
  display_name: "Prepare Next Slice"
  short_description: "Prepare one bounded review decision"
  default_prompt: "Use $prepare-next-slice to prepare one pending slice in the main context by default, add fresh preparation review when risk warrants it, and mark the bounded contract ready."
