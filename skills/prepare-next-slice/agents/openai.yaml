interface:
  display_name: "Prepare Next Slice"
  short_description: "Prepare one slice with execution fit"
  default_prompt: "Use $prepare-next-slice to prepare the next rolling-wave slice, decompose parallel chunks where appropriate, and identify agent-fit, human-fit, either, or hybrid work."
