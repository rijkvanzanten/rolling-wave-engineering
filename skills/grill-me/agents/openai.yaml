interface:
  display_name: "Grill Me"
  short_description: "Pressure-test plans until usable"
  default_prompt: "Use $grill-me to pressure-test this plan until usable; honor bounded prepare-review mode when invoked by prepare-next-slice."
